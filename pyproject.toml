[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rushi", email = "rushi6270@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "dotenv>=0.9.9",
    "langchain>=1.3.15",
    "langchain-openai>=1.4.3",
    "pymysql>=1.2.0",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
project = "project:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
